// import * as path from 'node:path';
import path = require('node:path');

export const UPLOAD_FILE_PATH = path.join(process.cwd(), 'apps/jobs/uploads');
// export const UPLOAD_FILE_PATH = path.join(process.cwd(), '/uploads');
